## working with json in pyhton

# Python provides a built-in json module to work with JSON data.
# /We can use the json.loads() method to convert a JSON string to a dictionary 
#  json.dumps() to convert a dictionary to a JSON string.



## parse json to dictionary

# In Python, a JSON string can be converted into a dictionary using the built-in json module using json.loads().

